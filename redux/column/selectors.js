export const selectAllColumns = (state) => state.columns.items;

export const selectLoading = (state) => state.columns.loading;

export const selectError = (state) => state.columns.error;

export const selectColumn = (state) => state.columns.currentColumn;
